import { Menu } from "@/components/menu";
import { DefaultLayout } from "@/components/Layout";
import React from "react";

const menu = () => {
  return (
    <DefaultLayout>
      <Menu />
    </DefaultLayout>
  );
};

export default menu;
